package inf.unideb.hu.chessgame;

/**
 * Hello world!
 *
 */
public class App 
{
/*
public static void main( String[] args ) throws InterruptedException {

        Board board = new SimpleBoard();


        Bishop bishop1 = new Bishop(new Tile(0, 2));
        Queen queen1 = new Queen(new Tile(1, 1));
        Rook rook1 = new Rook(new Tile(0, 1));
        Knight knight1 = new Knight(new Tile(2, 1));
        Knight knight2 = new Knight(new Tile(3, 0));


        board.placePiece(bishop1);
        board.placePiece(queen1);
        board.placePiece(rook1);
        board.placePiece(knight1);
        board.placePiece(knight2);

        rook1.move(new Tile(1, 1), board);
        bishop1.move(new Tile(2, 0), board);
        for (int i = 0; i < 4; i++) {
            System.out.println();
            for (int j = 0; j < 4; j++) {
                if (board.getTile(i, j).getPiece() != null)
                    System.out.print(board.getTile(i, j).getPiece().getName() + " ");
                else {
                    System.out.print("(" + i + "," + j + ")" + " ");
                }
            }
        }
        System.out.println();
        Thread.sleep(1000);
    }
 */
}
